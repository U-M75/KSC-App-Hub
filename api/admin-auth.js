import { clean, createAdminToken, configured } from './_lib/admin-auth.js'
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{if(!configured())return res.status(500).json({error:'APP_HUB_ADMIN_PASSWORD is not configured.'});if(clean(req.body?.password)!==clean(process.env.APP_HUB_ADMIN_PASSWORD))return res.status(401).json({error:'Incorrect admin password.'});return res.status(200).json({success:true,token:createAdminToken()})}catch(e){console.error('App Hub auth error:',e);return res.status(500).json({error:e.message||'Admin login failed.'})}}
